#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../winresourcer/lib/WinResourcer" "$@"
  ret=$?
else 
  node  "$basedir/../winresourcer/lib/WinResourcer" "$@"
  ret=$?
fi
exit $ret
